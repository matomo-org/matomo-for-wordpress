<div class="languageSelection" ng-cloak menu-title="Sites" piwik-menudropdown>
    {% for site in sites %}
        <a class="item {% if currentId == site.id %}active{% endif %}" href="{{ site.url|e('html_attr') }}"> {{ site.name }} (Site ID: {{ site.id }})</a>
    {% endfor %}
</div>